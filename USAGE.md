# Nexus 使用指南

## 1. 启动系统

### 1.1 启动后端服务

```bash
cd d:\NEXUS
node scripts/dev.js
```

等待所有 18 个服务启动完成（约 5 秒），终端会显示每个服务的监听端口。

### 1.2 启动前端

```bash
cd d:\NEXUS\client
npx vite --port 5173
```

### 1.3 启动 Electron 桌面客户端（可选）

```bash
cd d:\NEXUS\client
npx electron .
```

### 1.4 验证启动

访问 http://localhost:8080/api/services/health，确认 17+ 服务在线。

## 2. 登录

打开 http://localhost:5173，使用以下账号登录：

| 用户名 | 密码 | 角色 |
|--------|------|------|
| admin | Admin@1234 | 管理员 |
| liuyang | Nexus@1234 | 员工 |

## 3. 功能模块使用

### 3.1 工作台

登录后默认进入工作台，展示：
- 欢迎横幅（含服务在线状态）
- 待办事项、今日日程、考勤打卡快捷卡片
- 应用中心（13 个模块入口）

### 3.2 即时通讯 (IM)

- 点击侧边栏「即时通讯」
- 创建会话：通过 API 创建单聊/群聊
- 发送消息：在聊天输入框输入文字，点击发送
- 消息类型：支持文字、表情、文件、图片、语音
- 实时同步：WebSocket 推送，已读回执、输入中提示

### 3.3 视频会议

- 点击「立即发起会议」创建即时会议
- 系统会请求摄像头/麦克风权限
- 控制栏：静音、摄像头开关、屏幕共享、录制、举手、离开
- 会议聊天：底部聊天区发送文字消息
- WebRTC P2P 直连，无需第三方 SFU

### 3.4 关系图谱

- 点击「关系图谱」进入
- 自动加载万级节点数据（需等待 5-10 秒播种）
- 交互：
  - 滚轮缩放
  - 拖拽平移
  - 双击聚焦子图
  - 搜索节点
  - 类型筛选
- 性能指标：左下角显示 FPS、节点数、边数

### 3.5 文档协作

- 点击「文档协作」
- 「+ 新建文档」创建文档
- 实时协同编辑：Yjs CRDT 自动同步
- 在线协作者头像显示在标题栏
- 自动保存（2 秒防抖）

### 3.6 流程审批

- 五个标签页：待我审批、我已审批、我发起的、流程定义、表单定义
- 审批操作：同意、拒绝、转交
- 流程定义：创建 BPM 流程
- 表单定义：设计审批表单

### 3.7 日程管理

- 月视图日历
- 「+ 新建日程」创建事件
- 左右箭头切换月份
- 「今天」回到当前月

### 3.8 云盘文件

- 网格/列表视图切换
- 「⬆ 上传」上传文件
- 点击文件夹进入子目录
- 点击文件下载
- 🗑 删除文件

### 3.9 项目管理

- 项目列表 → 点击进入项目
- 三种视图：看板、列表、甘特图
- 看板：拖拽任务卡片切换状态
- 「+ 任务」创建新任务

### 3.10 考勤管理

- 实时时钟显示
- 「一键打卡」GPS 打卡
- 本月统计：出勤天数、迟到、早退
- 打卡记录表格

### 3.11 通讯录

- 左侧部门树
- 搜索人员
- 点击人员卡片查看详情
- 手机号脱敏显示

### 3.12 企业论坛

- 分区筛选：公司动态、技术分享、招聘内推等
- 「+ 发帖」发布帖子
- 点赞、评论

### 3.13 数据分析

- 日活跃用户趋势（柱状图）
- 会议统计（饼图）
- 审批效率、考勤统计
- 数据大屏（总用户数、消息数、会议数、文档数）

### 3.14 AI 助手

- 对话式交互
- 快捷操作：总结文档、翻译、智能搜索、推荐会议时间
- AI 生成内容标注来源

### 3.15 设置

- 个人资料：查看用户信息
- 安全设置：修改密码、MFA、退出登录
- 外观：亮色/深色主题切换
- 关于：版本信息

## 4. 调试

### 4.1 服务调试端点

```bash
curl http://localhost:8081/debug    # auth
curl http://localhost:8083/debug    # im
curl http://localhost:8087/debug    # knowledge
```

### 4.2 前端调试对象

```javascript
window.__nexus.user    // 当前用户信息
window.__nexus.token   // JWT 令牌
window.__nexus.api     // API 客户端
```

### 4.3 日志查看

```bash
# 服务日志
type d:\NEXUS\data\auth.log
type d:\NEXUS\data\im.log

# 错误日志
type d:\NEXUS\data\auth.err
```

## 5. 测试

```bash
# 全部测试
npx playwright test

# 指定模块
npx playwright test tests/e2e/auth.spec.js

# 生成报告
npx playwright show-report
```
