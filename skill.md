---
name: fullstack-one-shot
description: 单轮对话交付完整全栈项目，内置安全与合规守护。当用户要求"做一个网站 / 应用 / 系统 / 全栈项目"，或明确表示"直接做完、别问我、自己测试、自己截图、注意安全"时使用。覆盖需求内化、技术选型、全量编码、运行启动、浏览器自体验、安全漏洞自审、法律责任核查、联网搜索排障、质检交付全流程。
---

# 单轮全栈交付·安全增强版

## 使命

用户一句话提出需求，你独立完成整个项目：设计 → 编码 → 运行 → 体验 → 检查 → 安全审计 → 合规核查 → 修复 → 交付。中途**不向用户提任何问题**，所有决策采用保守默认值并记录在案，最终一次性交付可运行、经过安全审计的项目与质检报告。

## 铁律（违反任何一条即视为任务失败）

1. **不反问**：信息缺口一律用"保守默认值 + 假设清单"补齐，写入最终报告。
2. **必须自体验**：必须在浏览器中真实打开页面、执行核心操作、截图留证，才允许宣称完成。
3. **报错即修**：终端、控制台、依赖审计出现任何 error，立即进入修复循环，直到 0 error。
4. **证据先行**：每一项"已完成"的断言必须附带证据（截图 / 日志 / 测试输出）。
5. **安全左移**：编码阶段即按安全默认值实现（参数化查询、输出转义、环境变量管密钥），审计阶段逐项核对，禁止"先写完再补安全"。
6. **只做防御**：全程禁止编写漏洞利用代码；安全自测仅限用自己的恶意样本验证自身防御是否生效。
7. **法律问题三分法**：技术侧可整改的自动整改；需办理的（备案、资质）列移交清单；法律文本仅输出草案并声明建议专业人士审定，禁止声称项目"已合规"。
8. **预算控制**：功能与安全修复共享总预算 12 轮。超预算启动降级策略（简化或下线该功能），而非交付残次品。
9. **零幻觉依赖**：不确定的 API 用法、包版本、CVE 编号、法规要求，必须先联网搜索确认，禁止凭记忆编写或断言。

## 执行流程

### Phase 0 · 需求内化（静默完成，不提问）

输出 SPEC 卡：

- **产品一句话**：做什么、给谁用、解决什么问题。
- **核心用户旅程**：≤ 5 条，按优先级排序，这是必须跑通的验收标准。
- **功能范围**：MVP 必做 / 假设加分 / 明确不做。
- **合规雷达**：标记项目是否涉及以下任一触发器——收集个人信息、用户注册登录、UGC 内容、支付、生成式 AI、地图、面向大陆公众的网站服务。每个触发器映射到 Phase 5 的 L 系列核查项。
- **假设清单**：所有自行拍板的决策逐条列出。

### Phase 1 · 技术选型（按规模匹配，禁止过度工程）

| 项目规模 | 技术栈 | 启动方式 |
|---|---|---|
| 轻量工具 / 单页演示 | 单文件 HTML + 内联 CSS/JS（可 CDN 引入框架） | 浏览器直接打开 |
| 中型前端应用 | Vite + React + TypeScript | `npm run dev` |
| 全栈应用（含 API） | Next.js（App Router）+ TypeScript | `npm run dev` |
| 需要数据持久化 | 上述任一 + SQLite（better-sqlite3）或 localStorage | 同上 |

选型原则：能用静态页面解决的不上框架，能上 SQLite 的不要求用户装数据库。

### Phase 2 · 编码（一次性全量生成 + 安全内建）

1. 先输出完整文件树，再逐文件写入，禁止"其余文件类似"式省略；每个文件写完立即保存。
2. 依赖版本使用搜索确认过的稳定版本，锁死主版本号。
3. 代码必须包含：错误边界 / try-catch、加载态、空态，禁止只写 happy path。
4. **安全内建清单**（编码时同步落实）：数据库一律参数化查询；渲染用户输入一律转义；密钥一律环境变量并提供 `.env.example`；密码一律 bcrypt/argon2 加盐哈希；所有接口做输入校验；`.gitignore` 覆盖 `.env` 与依赖目录。

### Phase 3 · 启动与冒烟测试

1. 安装依赖：`npm install`（失败换 pnpm / yarn，再失败进修复循环）。
2. 启动开发服务器，记录端口。
3. 健康检查：首页 HTTP 200；后端 API 用 curl 逐个验证核心接口。
4. **依赖审计**：执行 `npm audit`（或对应包管理器命令），高危及以上漏洞计入问题清单。

### Phase 4 · 浏览器自体验协议（不可跳过）

每一步截图留证：

1. **首屏检查**：确认渲染完整、无白屏、无布局破碎。
2. **控制台检查**：任何 error 或 warning 记入问题清单。
3. **核心旅程走查**：按 Phase 0 用户旅程逐条真实操作，每个关键节点截图。
4. **网络检查**：Network 面板无 4xx/5xx 请求。
5. **边界操作**：一次非法输入 / 空提交，确认错误处理符合预期。
6. **攻击面自测**（仅测自己项目）：向输入框提交 `<script>alert(1)</script>` 与 `' OR '1'='1` 等样本，确认被转义或拒绝；登录后手动改 URL 中资源 id 尝试访问他人数据，确认被权限拦截。全部留截图。

### Phase 5 · 安全与合规审计

**S 系列 · 技术漏洞逐项核对**（每项记录位置与风险等级）：

- S1 注入类：SQL/NoSQL 注入、XSS、命令注入、路径遍历
- S2 认证授权：明文密码、硬编码 JWT 密钥、无鉴权接口、IDOR 越权、会话不过期
- S3 敏感暴露：源码/git 历史中的密钥、`.env` 可公网访问、API 过度返回字段、前端存储密码
- S4 依赖安全：审计输出中的已知漏洞与弃用包
- S5 传输配置：HTTPS、混合内容、CORS 通配、安全响应头（CSP 等）、生产 debug 关闭
- S6 数据保护：无隐私告知的个人信息收集、敏感数据明文落库、日志打印敏感字段、上传无限制

**L 系列 · 法律责任核查**（对照 Phase 0 合规雷达逐条处理）：

- L1 ICP 备案（面向大陆公众的网站服务，未办理列入移交清单）
- L2 个人信息保护（隐私政策、最小必要、查询/更正/删除途径、敏感信息单独同意）
- L3 数据安全（分级管理、重要数据境内存储）
- L4 UGC 内容（审核机制、举报入口）
- L5 特殊功能资质（生成式 AI 备案与标识、支付资质、地图审图号、素材版权）
- L6 营销表述（绝对化用语、未成年人保护）

**风险分级**：P0 严重（可远程利用或触法律红线，修复前不得交付）＞ P1 高（敏感暴露、明文密码）＞ P2 中（安全头缺失、低危 CVE）＞ P3 建议。

### Phase 6 · 修复循环算法

```
problems = 合并 Phase 3/4/5 发现的功能问题、漏洞与合规技术项
budget = 12

while problems 非空 and budget > 0:
    problem = 按 P0 > P1 > 功能阻塞 > P2 > P3 取最高优先级
    if 能定位原因:
        修复 → 重启/热更新 → 用原样本复测受影响路径 → 截图对比
    else:
        联网搜索（报错原文 / CVE 编号 / 法规条目 + 技术栈名 + fix/solution）
        if 仍无法解决:
            降级：简化或下线该功能，记录进"已知限制"
    budget -= 1

if budget == 0 and problems 仍非空:
    全部转为"已知限制"写入报告，禁止隐瞒
```

### Phase 7 · 联网搜索协议

以下情况必须搜索：报错无法定位；第三方库 API 签名不确定；需要真实素材；版本兼容冲突；**CVE 或依赖漏洞详情；法规与备案要求的最新口径**。搜索结果须核对来源与时效，弃用过时方案。

### Phase 8 · 交付报告（固定格式）

```
## 交付报告
### 项目概览
[一句话 + 技术栈 + 文件树]
### 启动方式
[精确命令，复制即可运行]
### 功能验收表
| 功能 | 状态 | 证据（截图编号） |
### 安全审计表
| 编号 | 类别 | 位置 | 风险等级 | 修复措施 | 复测证据 |
### 合规核查表
| 条目 | 状态 | 处理方式 |
### 移交用户办理事项
| 事项 | 原因 | 建议动作 |
### 截图证据
[首屏 / 核心操作 / 边界情况 / 攻击样本复测]
### 假设与已知限制
### 建议的后续迭代（≤ 3 条）
### 声明
本交付包含自动化代码级安全检查，不能替代专业渗透测试与法律意见。
```

## 反模式（禁止行为）

- 写完代码就说"已完成"而未在浏览器验证。
- 因报错把问题抛回给用户（"你可以试试…"）。
- 用 TODO 注释、`// 此处省略` 占位交付。
- 同时引入功能重叠的库。
- 询问用户"你希望用什么技术栈 / 要不要加登录"——自行决定并记录假设。
- 为演示方便注释掉鉴权、跳过输入校验、临时打开 CORS `*`。
- 用真实密钥或他人真实个人信息做测试数据。
- 声称项目"完全合规"或替用户承诺法律结论。
