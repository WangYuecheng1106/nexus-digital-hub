# 严重问题（深度扩写）

> 调研日期：2026-08-13  
> 证据：Playwright 体验机器人 `npm run experience` → `screenshots/experience/` + `data/experience-report.json`  
> 竞品：钉钉官网 / 悟空·Agent OS / WeLink / 飞书 / Viva Engage（联网）+ 既有 `COMPETITOR_DEEP_DIVE.md`  
> 钉钉桌面：本机 `%APPDATA%\DingTalk` 存在（含 `wukong`），当前 **DingTalk.exe 未在跑**；UI-TARS 源码在 `E:\UI-TARS-desktop-0.3.0`，无视觉模型 endpoint 时无法真实点桌面——清单见 `npm run experience:dingtalk`  
> **未改** `readme.md` / `report.md`。

---

# 1. 审美问题

### 现象（截图）

| 画面 | 文件 | 观感问题 |
|------|------|----------|
| 落地页 | `00-landing.png` | 对标叙事清楚，暗色 + 蓝强调可用，但仍是「功能卡片墙」，没有 Motion/Aceternity 级入场、光晕、3D 深度 |
| 工作台首帧 | `01-workbench.png` | 曾长时间停在「加载中…」——首屏空壳比丑更致命 |
| 工作台就绪 | `mod-workbench.png` | 问候 + 三卡 + 应用中心已像钉钉工作台骨架，但卡片无阴影层次、无微动效、侧栏纯图标无字 |
| 会议 | `mod-meeting.png` | 一张居中卡片，四周大片死黑，不像产品更像占位 |
| 文档 | `mod-document.png` | 两张同名 `demo` 卡片漂在虚空里，时间还显示 **2028** |
| 项目 | `mod-project.png` | 四张几乎同名「测试项目」/「x」，无进度、成员、状态色 |
| 分析 | `mod-analytics.png` | 图表能看，下半屏空；无日期筛选、无下钻 |
| 设置顶栏 | `mod-settings.png` | 人在「设置」，顶栏仍写「工作台」 |

钉钉 6.0 以后的审美策略是 **删繁就简、统一交互骨架**（消息/DING/文档一套控件）。飞书赢在留白与排版。Aceternity / React Bits / Universe 赢在 **光、运动、材质**。我们现在停在「深色 IDE 皮肤」：能看，但不「值钱」。

### 对标要点

- **钉钉**：工作台九宫格有应用名、角标、分组；消息气泡有尾巴、语音有波形；DING 页极简。
- **飞书**：会话列表信息密度高但不挤；空态有插画 + 一句行动建议。
- **WeLink**：安全水印、围栏是「安全感」视觉，不只是 logo。
- **Motion / Aceternity / React Bits**：按钮 `active` 缩放、面板 180ms 缓入、骨架屏 shimmer——我们 CSS 已有一部分（Toast/cmdk），**模块页几乎没用上**。

### 解决方案（落地，不只是「去网站下资源」）

1. **首屏禁止「加载中」独占**：工作台骨架屏（三卡 + 九宫格灰块）必须先画出来，数据后填。`01-workbench.png` 是 P0。
2. **侧栏补 tooltip 已有，补「悬停展开名称」**（钉钉窄栏 → 宽栏）。纯图标 16 个模块，新人无法扫读。
3. **空态分级**：云盘「暂无文件」要带「上传 / 新建文件夹」双 CTA（现在只有上传）。日历空月要有「今天还没有日程，点右上角新建」。
4. **运动约束**：沿用 `global.css` 已写的 `slideInUp` / `shimmer` / `prefers-reduced-motion`；禁止每个模块再发明一套 inline 动画。
5. **品牌**：落地页可以保留 Cursor 气质，但登录后工作台应更「办公」——卡片 elevation、应用磁贴 hover 上浮 1px（`.app-tile` 已写，确认工作台用了没有）。

### 验收

- 刷新工作台：200ms 内看到骨架，不再是居中「加载中…」
- 任意空模块：有插画/图标 + 一句说明 + 一个主按钮
- 顶栏标题 = 当前模块名（设置页不得再写工作台）

---

# 2. 细节不到位（如点击对话框没有弹窗）

### 现象（机器人实点）

| 操作 | 结果 | 严重度 |
|------|------|--------|
| 消息悬停 | 出现 回复/复制/转发/转待办 | 已修好 |
| 消息右键 | `.ctx-menu` 弹出（`probe-im-ctx.png`） | 已修好 |
| 转为待办 | toast「已加入待办」，工作台待办数增加 | 已修好 |
| Ctrl+K | 命令面板（`probe-cmdk.png`） | 已修好 |
| 新建日程 / 发帖 | Modal + ESC 关闭 | 已修好 |
| **IM 附件** | **无文件选择、无弹窗** | **P0 死按钮** |
| **IM 图片** | **同上** | **P0 死按钮** |
| 项目页 | 找不到「新建任务」（只有「新建项目」） | P1 |
| 点邮件列表 | 右侧仍「选择一封邮件」风险 | P0/P1 |
| 论坛板块数字 | 全是 0，列表却有帖 | P1 |
| 通讯录组织树 | 「Nexus 集团」渲染两遍；部门 (0)；刘洋职位 `?????` | P1 |

钉钉桌面的标准是：**看得见的控件一定有下文**。长按/右键消息 → 钉选、DING、转待办、转日程、转发。附件点开就是系统文件框。我们 composer 上的回形针和图片是 **装饰**。

### 同类产品细节清单（必须逐项对齐）

**消息（钉钉 / 飞书 / 企微）**

- 已读：单聊「已读/未读」；群聊已读人数，点开名单；@ 时被 @ 者已读直接显示
- 钉选：右键「钉选此消息」，顶栏黄条/图钉；飞书有 Pin + 二次确认
- DING：应用内 / 短信 / 电话；接收方确认 = 责任转移（README 要求了，产品没有）
- 语音：时长 + 波形 + 播放进度（我们有气泡组件，会话空态才写「钉钉风格气泡」，列表里很少见到）
- 引用回复、合并转发、多选、收藏、表情/GIF
- 草稿：切会话不丢

**审批（人人都是产品经理拆过钉钉 OA）**

- 审批以 **工作通知进会话** 投递，不是藏在独立 Tab
- 表单：拖拽控件 + 字段权限（某节点可改、可隐）
- 流程：条件分支、会签、加签、转交、退回
- 使用端：工作台 → OA → 选模板 → 填表 → 提交；PC/手机同一套
- WeLink **飞羽**：30+ 通用模板；**蒲公英**：30 种表单

**工作台 / 搜问**

- 钉钉 ONE：「事找人」卡片流；长按语音指挥 Agent
- AI 搜问：搜人/事/知识/进展，按权限聚合聊天+文档+周报
- 我们的 Ctrl+K 只能跳 **模块和快捷操作**，不能搜消息正文/文件

### 解决方案

1. **消灭死按钮**：附件/图片必须挂隐藏 `<input type="file">`，选完走 IM 文件/图片消息（README 消息类型 2、5）。
2. **GitHub skill 能加速的是「弹层/焦点陷阱/右键」**——这部分已有 `Modal` / `ContextMenu` / `Toast`；剩下是 **业务绑定**，skill 替代不了。
3. **每个 icon-button 的契约**：`title` 有文案 ⇒ 必须有 `onClick` 或 `htmlFor`；体验机器人已对「附件/图片」做死按钮探测，以后加「表情/@/投票」。
4. **通讯录**：组织树去重、人数 = 右侧卡片数、角色名不得 `?????`。
5. **论坛**：`/forum/sections` 的 count 必须跟列表同一套 `section` key（之前前后端 key 对不齐就会全 0）。

### 验收

- 点附件 → 系统文件框 → 发出文件气泡
- 点图片 → 发出图片气泡，点击可放大
- 点邮件 → 右侧出现正文，不是空态
- 组织树不重复，部门人数 > 0

---

# 3. 功能不好用 / 不能用

### 现象（截图 + 机器人）

| 模块 | 能用的 | 不能用 / 不好用 |
|------|--------|-----------------|
| IM | 发文字、撤回、好友 Tab、转待办 | 附件/图片/DING/@/群管理/已读名单 |
| 邮箱 | 演示收件箱 4 封、MCP 4 工具文案 | 点开正文弱；真实 IMAP 未作为主路径 |
| 会议 | 「立即发起会议」按钮 | 无会中画面、无共享、无录制、无听记 |
| 文档 | 新建、两份 demo | 点开协同未知；时间 2028；无评论/版本 UI |
| 云盘 | 上传按钮 | 空盘；无文件夹、共享、版本、回收站 |
| 日历 | 月视图 + 新建弹窗 | 全月空白；无周/日；无会议室 |
| 审批 | Tab 齐全 | 「暂无待办」；模板少；无工作通知回流 IM |
| 图谱 | canvas 在、FPS 63、号称 10030 节点 | **视口节点空、画面全黑**（`mod-knowledge.png`）——README 评审重点 |
| AI | 能聊、有模型下拉 | 「提醒我下午 5 点交周报」→ 答审批配置；来源 **`[object Object]`**（`probe-ai-tool.png`） |
| 考勤 | 一键打卡有记录 | `check_in` / `gps` 未中文化 |
| 健康检查 | 多数服务 ok | 网关仍报 `user=down`（根本没有 user 服务） |

README 硬约束是「不得框架空壳」。图谱黑屏 + AI 答非所问 + 会议只有一张卡，就是空壳。

### 竞品功能深度（联网 2026）

**钉钉（AI 钉钉 2.0 / 悟空，2026-03）**

- 底层重写为 **DingTalk CLI**：IM、音视频、文档、审批、日程、差旅 → 上万条 AI 可调用命令，带权限与沙箱
- 不再用 Computer Use **模拟点击**，而是原生调用（这正是我们 UI-TARS 去「点钉钉」要学的差异：点 GUI 是体验课，产品上要学 CLI）
- 悟空：独立 Agent 应用；可连钉钉/Slack/微信/Teams；RealDoc 文件系统给 AI 试错回滚
- 2025-12 Agent OS「木兰」：ONE 工作流界面、AI 搜问、AI 表格、AI 听记、DingTalk Real 硬件、DEAP
- OA：审批消息进聊天；表单字段级权限

**WeLink**

- 连接团队 / 业务 / 知识 / IoT
- 小微助手：一句话直达应用
- 智能纪要、20 语种翻译
- 数据密盾 / 安全隧道 / 安全围栏
- 飞羽 30+ 审批模板、蒲公英 ~30 表单、开放 300+ API

**飞书**

- 工作流/云雀更灵活；文档与消息互链更顺
- 群置顶有 Pin + 防误关

**Viva Engage**

- 社区/知识层 + Copilot 摘要，不是 IM 替代品

### 解决方案

1. **图谱 P0**：视口节点为 0 时强制 `fitView` / 把种子节点画进相机；「AI 整理人员」必须让人看见点和边。
2. **AI sources**：`sources.join` 前 `map(s => s.title || s.id || String(s))`，禁止 `[object Object]`。
3. **会议**：发起后进入会中页（本地预览 + 假远端 + 共享/静音/挂断），不要停在营销卡。
4. **部署成软件**：`npm run dev` 已能起 Vite；Electron preload 已补。用户要的是「打开窗口就能用」，不是 GitHub Pages 静态站。
5. **体验机器人当门禁**：`npm run experience` 任 P0 > 0 不得宣称模块完成。

### 验收

- `mod-knowledge.png` 能看见节点，视口节点 > 0
- 「提醒我…」产生待办或日程，而不是 workflow RAG
- 发起会议后能看到自己的摄像头预览或明确的权限提示

---

# 4. 竞争力

### 钉钉已经走到哪（2026）

钉钉公开对标的不再是「多一个 IM」，而是：

1. **Agent OS**：Agent 是执行主体；ONE 是人机桌面；悟空是调度器；DEAP 是企业自建 Agent
2. **CLI 化**：把 11 年功能变成 AI 的技能包——第三方 Agent 也能调钉钉
3. **企业级安全**：权限取「用户 ∩ 提问人」交集；沙箱；Token 费用可视；反 OpenClaw 式误删邮件
4. **软硬一体**：A1 录音卡、Real 执行器
5. **业财人事销套件**：合同、差旅、人事、营销，不是纯协作

WeLink 的差异化是 **安全四件套 + 小微 + 飞羽模板**。飞书是 **文档+开放工作流**。Viva 是 **社区知识**。

### Nexus 现在的「竞争力」实话

- 界面上贴了 **AgentOS** 标签（`mod-ai.png`）
- 快捷短语有「发消息 / 创建待办」
- 实际：本地 RAG 把「提醒我交周报」检索成 `doc_workflow_001`
- 没有 CLI、没有权限沙箱、没有 Skill 市场、没有 ONE 事找人流

这是 **叙事超前、能力滞后**。竞品空窗（我们可以抢的）其实是：

| 空窗 | 为何钉钉/WeLink 还不成熟 | Nexus 可做的最小版本 |
|------|--------------------------|----------------------|
| 开源可自托管的 Agent OS | 悟空要专属版/硬件 | 把现有 16 个 HTTP 服务收成 **Nexus CLI**（`nexus todo add` / `nexus im send`），给 AI 调 |
| 关系图谱当组织操作系统 | 钉钉没有万级人-事-文图 | **先让图画出来**，再让「谁在批我的假」一跳 |
| 统一端无老板/员工分裂 | 钉钉长期「已读+打卡」对立 | 我们已经统一端，要继续站在「帮员工执行」 |
| Computer Use 对照课 | UI-TARS 可用来抄钉钉细节 | 本仓库 `scripts/ui-tars-dingtalk.mjs` |

### 解决方案

1. **先做「对话即操作」6 个工具**（与 README / 悟空 CLI 同构）：发 IM、建待办、建日程、发起审批、搜图谱、写信。成功时画工具卡片，失败时说人话。
2. **不要再扩模型品牌墙**，直到这 6 个工具在离线 RAG 下也能走规则引擎（关键词 → tool，不必等千问 Key）。
3. **ONE 流**：工作台待办卡片点「去处理」必须跳到源模块（消息/审批/日程），现在待办标题是 `待办消息-89296` 这种机器名。
4. **UI-TARS 用法**（本机）：
   - 安装/打开钉钉桌面
   - 打开 `E:\UI-TARS-desktop-0.3.0` 的 Desktop，或 `npx @ui-tars/cli start`（需 `UITARS_BASE_URL` + `UITARS_API_KEY`）
   - 把 `data/dingtalk-uitars-plan.json` 里的 `instruction` 贴进去
   - 走完 10 个模块，把「Nexus 必须抄的 20 个细节」回写本文件

### 验收

- 对「发消息给张三说会议改到 3 点」出现 tool_call 且 IM 里真有这条
- 工作台待办可点击跳转，标题是人话

---

### 4.1 本机钉钉实机（2026-08-13 截屏）

文件：`screenshots/experience/dingtalk-desktop-primary.png`  
进程：`C:\Program Files (x86)\DingDing\DingtalkLauncher.exe`，用户目录含 **悟空**；待办角标 **2**。

当前钉钉窗口（账号侧栏可见「西安市经…」）已经不是旧版「消息+打卡」，而是 **AI 原生信息架构**：

| 侧栏入口 | 钉钉在卖什么 | Nexus 对应 |
|----------|--------------|------------|
| 顶栏「搜索或提问 Ctrl+Shift+F」 | 搜问，不是只搜聊天 | 我们是 Ctrl+K 跳模块 |
| **千问办公** | 旗舰对话入口，和 IM 平级 | AI 藏在侧栏最底 |
| **消息** | 协同主场 | 有，缺 DING/钉选/附件 |
| **AI 表格** | 工作入口（本屏正在「数据」：TODO 跟进、导入 Excel、收集表） | 无 |
| **AI 听记** | 会议/录音转写 | 会议模块无听记 |
| **工作台** | 应用操作系统 | 有九宫格，缺 OA 深链 |
| **通讯录 / 会议 / 日历** | 独立一级 | 有，偏空 |
| **待办**（红点 2） | 一级入口，事找人 | 塞在工作台一张卡里 |
| **DING** | 强触达，和消息分开 | 完全没有 |

结论：竞争力差距首先是 **信息架构**。钉钉把「提问、表格、听记、待办、DING」抬到和消息同一级；我们把 16 个模块平铺成图标，AI 还在用 RAG 把「提醒我」答成审批文档。

UI-TARS：源码就绪，**未配置视觉模型 Key**，无法自动点完上述侧栏。请打开 UI-TARS Desktop，粘贴 `data/dingtalk-uitars-plan.json` 的 `instruction`，按 10 步走查。Magic Mouse 本次超时，改用全屏截图。

---

# 5. 不成体系（功能之间不能互相连通）

### 钉钉的中心不是「模块多」，是「事在消息里闭环」

苏星河系列视频的判断：钉钉是组织操作系统；LLM 用来「扶贫」不会用软件的人。产品结构是：

```
消息（工作通知） ← 审批 / 日程 / 文档 / 打卡 / DING
       ↓
   转待办 / 转日程 / 钉选 / 打开文档
       ↓
   AI 搜问 / 悟空 CLI 执行
```

WeLink 是「连接四件套」。Viva 是社区知识。我们是 **16 个并列 Hash 路由**，侧栏平铺，模块之间只靠人记。

### 我们已接通 vs 未接通

| 链路 | 状态 |
|------|------|
| 消息 → 待办 | 通（右键 + toast + 工作台数字） |
| 消息 → 日程 / 文档 / 审批 | 无 |
| 审批通过 → IM 工作通知 | 无 |
| 会议 → 日历事件 | 无 |
| 日历 → 会议入会 | 无 |
| AI 指令 → 待办/IM/日历 | 断（RAG 抢答） |
| 图谱节点 → 打开人/文档/审批 | 画面都是黑的 |
| 命令面板 → 模块跳转 | 通 |
| 命令面板 → 搜聊天记录 | 无 |
| 邮箱 → 会话 | 无 |
| 论坛 @ / 话题 → 消息 | 无 |
| 健康检查 user | 假 down，污染工作台 17/18 |

### 解决方案（一个中心：Agent + 消息）

围绕 **「一句话 / 一条消息」能驱动论坛以外的所有动作**（用户原话：围绕 AI 构建论坛、图谱、审批、代办、发信息、写文件）：

1. **事件总线已经有** `publishEvent` / gateway 扇出。规定最小事件集：
   - `im.message.todo`（已有雏形）
   - `workflow.task_assigned` → IM 工作通知
   - `calendar.event_created` ← `meeting.created`
   - `ai.tool_invoked` 审计
2. **消息右键补齐钉钉四件套**：转待办（已有）、转日程、转审批、打开聊天里的文档链接。
3. **工作台 ONE**：待办、今日会、未读、待批，四张卡都是深链，不是死数字。
4. **图谱是索引不是孤岛**：节点双击 = `navigate(module)+id`；人员整理后边要连部门/项目/文档。
5. **部署**：本地 `npm run dev` + Electron；Pages 只做介绍。登录页网络错误不得再显示成「密码错误」（此项已修）。

### 验收

- 请假审批通过后，申请人会话出现「张三已同意」
- 发起会议后日历当天出现块
- 图谱点员工 → 通讯录/IM

---

# 6. 体验方法（机器人 + UI-TARS + 截图）

### 6.1 Nexus 深度体验机器人

```bash
npm run dev          # 另开终端
npm run experience   # Playwright 无头走 16 模块、点死按钮、截图
```

产出：

- `screenshots/experience/*.png`
- `data/experience-report.json`（P0/P1/ok）

本次截图结论摘要（机器人 **P0=4 / P1=6 / ok=17**）：

- 已通：命令面板、消息右键、转待办、日程/论坛弹窗、邮件可打开正文、工作台应用中心
- **P0**：IM 附件/图片死按钮；AI `[object Object]`；「提醒我」被 RAG 答成审批文档
- **P1**：论坛 count 全 0；通讯录树重复 + 人数 0 + 职位 `?????`；审批空；项目无「新建任务」
- **目视覆盖机器人**：图谱文案写 10030 节点，截图仍是黑屏（视口正则被「1.00x」误匹配，不能当通过）

### 6.2 UI-TARS 体验本机钉钉

源码：`E:\UI-TARS-desktop-0.3.0\UI-TARS-desktop-0.3.0`（ByteDance GUI Agent，nut-js 控鼠标键盘）。

```bash
npm run experience:dingtalk
```

- 探测钉钉是否在跑、是否有 `wukong` 目录、写出 `data/dingtalk-uitars-plan.json`（10 模块对照表 + 完整 instruction）
- 若设置 `UITARS_BASE_URL` / `UITARS_API_KEY`，会 `npx @ui-tars/cli start -t nutjs` 真开 Computer Use
- **本次环境**：钉钉用户目录在、悟空目录在、`mcp_port` 曾指向 8440 但进程已死；**未找到可启动的 DingTalk.exe，也没有 UI-TARS 模型 Key**，故钉钉 GUI 走查改为「清单 + 公开资料」，不能假装已经点完每一个按钮

请你本地：打开钉钉客户端 → 打开 UI-TARS Desktop → 粘贴 json 里的 `instruction`。走查时重点抄：消息右键、DING、工作通知、OA 模板数量、ONE/悟空是否真执行。

Magic Mouse（`mm_status`）本次调用超时，未作为主路径。

### 6.3 设计站点（审美作业）

按你原方案继续看，但要带着「工作台/消息/空态」三个页面去抄，不要只下通用卡片：

- https://www.reactbits.dev
- https://ui.aceternity.com
- https://www.awwwards.com （motion sites）
- Universe / Anime.js 只用于 200ms 内微交互

---

# 7. 按 README 模块的缺口总表

（只列「严重问题」级，细节见上）

| README 模块 | 成熟度 | 对标 |
|-------------|--------|------|
| IM | 文字+好友+转待办 | 钉钉消息类型 10 种里我们大约 2 种 |
| Meeting | 入口卡 | 钉钉会议+听记 |
| Calendar | 空月历 | 钉钉/WeLink 日程+会议室 |
| Drive | 空盘 | 钉盘 |
| Document | demo 列表 | 钉钉文档+Yjs 宣称未在截图中被验证 |
| Workflow | 空待办 | 钉钉 OA + 飞羽 30+ |
| Knowledge | 黑屏 | 评审重点，当前失败 |
| AI | 聊天 | 悟空 CLI / 千问办公 / 小微 |
| Portal | 三卡+九宫格 | 钉钉工作台/ONE |
| Auth | 能登录 | 错误文案已区分网络/密码 |

---

# 8. 下一步（按 P0）

1. 图谱 `fitView` + 种子可见  
2. IM 附件/图片 input  
3. AI 规则路由（提醒/发消息/待办）+ sources 序列化  
4. 会议会中页  
5. 审批模板 ≥ 8 个并写入 IM 工作通知  
6. 通讯录树去重、人数、角色名  
7. 顶栏标题、论坛 count、邮件正文  
8. 你打开钉钉 + UI-TARS，把 20 条细节贴回本节  

完成 1–3 后再谈审美动效；否则动效只是给空壳化妆。
